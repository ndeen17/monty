#include "monty.h"
/**
 * _stack - this is the  function prints the top
 * @head: the stack head
 * @counter: the line_number
 * Return: there is no return
*/
void _stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	opc.sq_flag = 0;
}
